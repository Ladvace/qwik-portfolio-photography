import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <header class="w-full px-4 py-2 h-14 box-border">
      <h2 class="m-0">Amalia D'onofrio</h2>
    </header>
  );
});
